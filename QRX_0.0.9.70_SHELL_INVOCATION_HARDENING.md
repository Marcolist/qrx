# QRX 0.0.9.70 — Shell Invocation Hardening

AI prepare and verification helper scripts are invoked explicitly via `bash`, so source ZIP extraction does not depend on preserved executable bits. SHA-256 and packaged-resource verification remain mandatory.
